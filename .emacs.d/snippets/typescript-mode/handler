# -*- mode: snippet -*-
# name: handler
# key: handler
# --
import { Context, Callback } from 'aws-lambda'

export async function handler (event, context: Context, callback: Callback) {
  ${1:callback(null, 'hello world!')}
}
